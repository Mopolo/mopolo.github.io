{% extends "layouts/main.twig" %}

{% block content %}

    <div>
        <h2 class="text-2xl font-semibold {{ colors.textLightBg }}">
            <a href="{{ urls.til.href }}" class="font-black underline">TIL</a> {{ til.title }}
        </h2>
    </div>

    <div class="mt-4 md:mt-10">
        <div class="prose dark:prose-invert">
            {{ til.content|raw }}
        </div>
    </div>

{% endblock %}
