---
category: Tips
title: Find a machine's IP public address
---
For this we can use `curl`:
```bash
curl ifconfig.me/ip
```

The domain itself explains other available infos and can be used in the browser to display the current IP.
